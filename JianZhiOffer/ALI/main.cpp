#include <iostream>
#include <vector>
#include <string>
#include <numeric>
#include <limits>
#include <stdint.h>

using namespace std;


/*请完成下面这个函数，实现题目要求的功能*/
/*当然，你也可以不按照下面这个模板来作答，完全按照自己的想法来 ^-^ */
/******************************开始写代码******************************/
string Link(const vector < string >&inputLines ){
map<string, string>
string ans = "gcc –o myhello myhello.o –lmyprint";
return ans;

}
/******************************结束写代码******************************/


int main() {
    int64_t n;
    cin >> n;
    string s;
    getline(cin, s);
    vector<string> inputLines;
    for(int64_t i = 0; i < n; ++i) {
        getline(cin, s);
        inputLines.push_back(s);
    }
    cout << Link(inputLines) << endl;
    return 0;
}
